@model UserManagement.Web.Models.Users.UserListViewModel

<h2>User List</h2>

<div class="table-responsive">
    <table class="table table-striped">
        <thead>
            <tr>
                <th>Id</th>
                <th>Forename</th>
                <th>Surname</th>
                <th>Email</th>
                <th>Date of Birth</th>
                <th>Account Active</th>
                <th style="width: 1px;"></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model.Items)
            {
                <tr>
                    <td>@item.Id</td>
                    <td>@item.Forename</td>
                    <td>@item.Surname</td>
                    <td>@item.Email</td>
                    <td>@item.DateOfBirth.ToString("dd/MM/yyyy")</td>
                    <td>@(item.IsActive ? "Yes" : "No")</td>
                    <td>
                        <div class="dropdown">
                            <button type="button" class="btn btn-default dropdown-toggle" data-bs-toggle="dropdown" aria-expanded="false" id="btnToggleActions">
                                Actions
                            </button>
                            <ul class="dropdown-menu" aria-labelledby="btnToggleActions">
                                <li><a asp-controller="Users" asp-action="View" asp-route-id="@item.Id" class="dropdown-item">View</a></li>
                                <li><a asp-controller="Users" asp-action="Edit" asp-route-id="@item.Id" class="dropdown-item">Edit</a></li>
                                <li><a asp-controller="Users" asp-action="Delete" asp-route-id="@item.Id" class="dropdown-item">Delete</a></li>
                            </ul>
                        </div>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>

@if (Model.TotalPages > 1)
{
    <nav>
        <ul class="pagination">
            @for (int i = 1; i <= Model.TotalPages; i++)
            {
                <li class="page-item @(i == Model.CurrentPage ? "active" : "")">
                    <a class="page-link"
                       asp-controller="Users"
                       asp-action="List"
                       asp-route-page="@i"
                       asp-route-isActive="@(Model.IsActiveFilter)">
                        @i
                    </a>
                </li>
            }
        </ul>
    </nav>
}

<div class="d-flex justify-content-between align-items-center mt-3">
    <div class="btn-group" role="group">
        <a asp-controller="Users" asp-action="List"
           class="btn btn-default @(Model.IsActiveFilter == null ? "btn-primary" : "")">Show All</a>

        <a asp-controller="Users" asp-action="List" asp-route-isActive="true"
           class="btn btn-default @(Model.IsActiveFilter == true ? "btn-primary" : "")">Active Only</a>

        <a asp-controller="Users" asp-action="List" asp-route-isActive="false"
           class="btn btn-default @(Model.IsActiveFilter == false ? "btn-primary" : "")">Non Active</a>
    </div>

    <a asp-controller="Users" asp-action="Add" class="btn btn-primary">
        Add User
    </a>
</div>
